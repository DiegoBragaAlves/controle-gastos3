# Controle de Gastos Pessoais (Web)

Sistema em Python com Flask + PostgreSQL.

## Como rodar local:
pip install -r requirements.txt
python app.py

## Deploy (Render):
Start command:
gunicorn app:app
